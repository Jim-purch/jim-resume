# Nixpacks 配置 :: Coolify 自动构建 Next.js 站点
# - 使用 Node.js 22 LTS 构建
# - 生产模式通过 `next start` 启动（package.json 中 `serve` 脚本）

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci --include=dev || npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run serve"
